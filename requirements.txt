Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
Flask-Limiter==3.7.0
signxml==3.2.2
lxml==5.2.2
python-dotenv==1.0.1
Jinja2==3.1.4
cryptography==41.0.7      # capped <42 so the pinned pyOpenSSL below can install
pyOpenSSL==23.3.0         # signxml 3.2.2 imports OpenSSL.crypto.verify, removed in pyOpenSSL >=24
Werkzeug==3.0.3
flask-login
gunicorn==22.0.0
pyrad==2.4                # RADIUS auth/accounting simulator (BSD-3 — no GPL)
pyotp==2.9.0             # TOTP (RFC 6238) for RADIUS MFA (MIT)
qrcode==7.4.2            # QR codes for TOTP enrollment, SVG output (BSD)

# SCIM 2.0 (enabled via ENABLE_SCIM=true; pure additions — no SAML impact)
scim2-models>=0.6.12
scim2-filter-parser>=0.7.0
httpx>=0.27.0
